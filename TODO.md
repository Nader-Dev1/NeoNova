# TODO

- [ ] تحديث `assets/js/pdf.js` لإصلاح `drawUnitCard()` (منع أخطاء المتغيرات غير المعرفة)
- [ ] إعادة تصميم كارد الوحدة بحيث يحتوي عنوان + 2x2 status grid بأماكن ثابتة على A4
- [ ] توحيد رسائل/أسماء الحالة مع ألوان مناسبة عبر `statusLabel()` و `statusColor()`
- [ ] تحسين الـpagination: أي overflow للـunit card أو photos يعمل `addPage()` مع header جديد بدون قص
- [x] التحقق المنطقي من `drawUnitCard` و `drawPhotos` أن `y` يرجع صح بعد كل رسم

- [x] تجربة Export Zone PDF و Export Unit PDF

